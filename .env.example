# AI 接入参数。复制为 .env.local 后填写，重启服务生效。
#
# 与 novel-data-studio 共用同一套变量名 —— 两个应用放在同一台机器上时，
# 一处配置即可同时生效，不必维护两份密钥。
#
# AI 配置只从环境变量读取，不读、不写任何本地配置文件：
# 密钥不落盘，也不会因为配置结构遗留旧值而盖过新 Key。
# 设置页对 AI 参数为只读展示。

NOVEL_STUDIO_BASE_URL=https://api.openai.com/v1
NOVEL_STUDIO_MODEL=gpt-4o
NOVEL_STUDIO_API_KEY=

# 打分用的小模型（可选，留空则与主模型一致）。
# 逐章打分调用量最大，换成便宜的小模型能显著降低成本。
# 这一项是 analyzer 特有的，studio 没有对应配置。
NOVEL_STUDIO_SCORE_MODEL=gpt-4o-mini

# 只识别以上 NOVEL_STUDIO_ 前缀的变量。
# 不会读取 OPENAI_API_KEY 等通用变量 —— 开发机上这类变量常为别的工具而存在，
# 误当作本工具的配置会让界面莫名变成只读且指向错误的服务。
